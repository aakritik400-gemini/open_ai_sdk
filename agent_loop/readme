# The Agent Loop is the concept of running the same agent multiple times in a sequence, sending it different questions or inputs without recreating the agent each time.

# questions = [
#     "List all products",
#     "Calculate total for 2 Laptops",
#     "Recommend a product for me"
# ]

# for q in questions:
#     result = await Runner.run(
#         starting_agent=agent,
#         input=q,
#         context=context,
#         run_config=run_config
#     )
#     print(result.final_output)

# for q in questions: → This is the loop.

# Runner.run(...) → Each iteration calls the agent to answer one question.

# The same agent and context are used across multiple queries.

# So, the agent is looped over multiple questions, instead of creating a new agent every time.